const INITIAL_STATE = {
    data: {},
  };
  
  const signupReducer = (state = INITIAL_STATE, action: any) => {
    switch (action.type) {
      default:
        return state;
    }
  };
  export default signupReducer;
  